version:  "3.8"

services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: SecretPassword123!
      POSTGRES_DB: BankingAppDb
    ports:
      - "5433:5432"
      
        
    